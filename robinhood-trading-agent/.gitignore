node_modules/
dist/
*.tsbuildinfo

# Persisted safety/halt state, PDT trade log, learning weights — local,
# stateful, and must never be reset by a fresh checkout or deploy.
/state/

# Structured audit log (decisions, orders, learning updates, halt/resume)
runs/

# Per-cycle end-of-run PnL chart written by the orchestration session —
# regenerated every cycle, never a durable repo artifact.
paper-pnl-cycle.svg

.env
.env.local
